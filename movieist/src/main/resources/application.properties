spring.application.name=movieist

# Default local Atlas URI (used when no env var provided). For production, set MONGO_URI and MONGO_DATABASE environment variables.
# You can override the entire URI with the MONGO_URI env var, or set MONGO_USER / MONGO_PASSWORD / MONGO_CLUSTER separately.
# Example (Windows PowerShell):
# $env:MONGO_URI = 'mongodb+srv://user:pass@cluster.example.net/?retryWrites=true&w=majority'
# $env:MONGO_DATABASE = 'movie-api-db'

# Use a single MONGO_URI env var if present, otherwise fall back to the literal (development) URI.
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://garena284ff_db_user:A4f4VFRKKa6YqHnR@cluster1.9ihrkii.mongodb.net/?retryWrites=true&w=majority}
# Database name, can be overridden with MONGO_DATABASE env var
spring.data.mongodb.database=${MONGO_DATABASE:movie-api-db}

server.port=8080

# GENERATED 32-byte BASE64 SECRET (recommended). Replace with your own secure secret if desired.
jwt.secret=yQ1v8K9s3f4G7h2J5kLm9pQwErTyUiOpAsDfGhJkL0M=
jwt.expiration=3600000
